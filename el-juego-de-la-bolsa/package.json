{
  "name": "el-juego-de-la-bolsa",
  "private": true,
  "workspaces": ["client", "server"],
  "scripts": {
    "postinstall": "npm -w server run prisma:generate || exit 0",
    "dev": "npm -w server run dev & npm -w client run dev",
    "build": "npm -w server run build && npm -w client run build",
    "start": "npm -w server run start:prod & npm -w client run preview"
  }
}
